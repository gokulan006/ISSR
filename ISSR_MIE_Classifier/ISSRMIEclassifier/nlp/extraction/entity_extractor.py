"""
Entity Extraction for MIE Classification
"""

class EntityExtractor:
    """Extracts entities (countries, organizations, locations, etc.) from text."""
    def __init__(self, config):
        self.config = config
    def extract(self, text):
        """Extract entities from text (stub)."""
        return {} 